{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build tests",
            "type": "shell",
            "command": "cmake --build ./Tests/build --config Debug",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
